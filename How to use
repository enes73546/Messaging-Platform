To use it you must run the server first then run all your clients after (do everything in a different terminal)
best if you use multiple terminals resized so you can see everything and the servers's terminal will track everything happening
